/*
  To find the remainder of two numbers, use the remainder '%' operator.

  The remainder operator is not to be confused with the modulus operator, as the remainder operator does not work with negative values
*/

// Only change code below this line

var remainder = 11 % 3;
