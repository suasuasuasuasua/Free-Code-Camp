/*
  JS defines a product compound operator '*', which multiplies a variable's current value by a number, then reassigns the resulting number.
*/

var a = 5;
var b = 12;
var c = 4.6;

// Only change code below this line
a *= 5;
b *= 3;
c *= 10;
