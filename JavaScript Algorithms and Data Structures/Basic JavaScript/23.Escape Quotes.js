/*
  To declare quotes inside of a string literal, we need to 'escape' it.

  To do this, you put a '\' before the quote, which denotes that the quotation is not the end of the string literal, but a quotation itself inside of the string literal.
*/

var myStr = "I am a \"double quoted\" string inside \"double quotes\"."; // Change this line
