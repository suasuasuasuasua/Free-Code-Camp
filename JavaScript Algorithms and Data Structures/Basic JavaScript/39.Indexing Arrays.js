/*
  To access content within an array, we index it similarly to how we would index a string.

  Instead of characters, we are returned data from within the data.
*/

var myArray = [50,60,70];

var myData = myArray[0];
