/*
  The value of one variable can be assigned to another variable.

  Thus, both variables will contain (point to the) same value
*/

// Setup
var a;
a = 7;
var b;

// Only change code below this line

b = a;
