/*
  To append to an array, we use the .push() method.

  .push() recieves one or more arguments, then pushes those arguments to the end of the array.
*/

// Setup
var myArray = [["John", 23], ["cat", 2]];

// Only change code below this line

myArray.push(["dog", 3])
