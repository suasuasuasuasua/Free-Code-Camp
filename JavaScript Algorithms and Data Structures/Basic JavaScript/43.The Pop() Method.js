/*
  .pop() is a method that removes the last element from an array and returns it.

  In this assignment, we '.pop()' the last element from myArray, which is ["cat", 2], then we assign it to remogedFromMyArray.

  myArray now only has one element, which happens to be an array.
*/

// Setup
var myArray = [["John", 23], ["cat", 2]];

// Only change code below this line
var removedFromMyArray = myArray.pop();
