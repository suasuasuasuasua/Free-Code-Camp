/*
  Functions can have parameters and arguments. Using these will make your functions rich and complex.

  Parameters are placeholder values that the function will use inside of its body.

  Arguments are the actual values that the user passes into the function.
*/
function functionWithArgs( x, y ) {
  console.log(x + y);
}
functionWithArgs(2,3);
